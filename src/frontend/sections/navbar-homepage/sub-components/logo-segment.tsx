import Link from "next/link";

const LogoSegment = () => {
  return (
    <div
      className="h-full flex items-center font-rozhaOne text-[1.25rem] tracking-wide">
      <Link href="/#services">Solutioner</Link>
    </div>
  );
};

export default LogoSegment;
